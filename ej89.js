let numero = 0;
let sumaParcial = 0;
for (let numero = 0; numero <= 1000; numero++) {
    if (sumaParcial += numero) { //Es lo mismo que poner SumaParcial = sumaParcial + numero;
       console.log('Suma parcial:', sumaParcial); 
    } 
}
    