let nombre = 'Gaston';
let apellido = 'Mosquera';
let template1 = `Mi nombre es ${nombre} y tiene ${nombre.length} letras`;
let template2 = `Mi apellido es ${apellido} y tiene ${apellido.length} letras`;
console.log(nombre.length);
console.log(apellido.length);
console.log(template1);
console.log(template2);