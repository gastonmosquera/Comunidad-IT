let numerosPares = function() {
    for(let numero = 0; numero <= 100; numero++) {
        if ((numero % 2) == 0) {
            console.log('Numeros pares:', numero);
        }
    }
}
numerosPares();
numerosPares();
numerosPares();
numerosPares();
numerosPares();